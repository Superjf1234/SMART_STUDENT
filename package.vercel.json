{
  "name": "smart-student-vercel",
  "version": "1.0.0",
  "description": "Smart Student - Aplicación educativa inteligente",
  "scripts": {
    "dev": "reflex run --env dev",
    "build": "reflex build --env prod",
    "start": "reflex run --env prod",
    "export": "reflex export",
    "vercel-build": "reflex build --env prod"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
